/*
 * @lc app=leetcode.cn id=62 lang=javascript
 *
 * [62] 不同路径
 */

// @lc code=start
/**
 * 解题思维：
 * 使用递归的思想
 * 递归公式：f(m,n) = f(m. n-1) + f(m-1, n)
 * 终止条件: f(1,2) = 1, f(2,1) = 1 
 * @param {number} m
 * @param {number} n
 * @return {number}
 */
var uniquePaths = function(m, n) {
    
};
// @lc code=end

